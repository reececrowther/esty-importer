# Base environment template. Same code runs locally and on the live server.
#
# LOCAL:  Copy .env.local.example to .env.local and set DATABASE_URL (and URLs) there.
#         Next.js uses .env.local overrides — no need to edit this file when switching.
# PRODUCTION: Set all variables in your host (e.g. Vercel → Settings → Environment Variables).
#         Never commit real secrets; .env and .env.local are gitignored.
# ---

# AI API Configuration (use one of the following)
# Option 1: OpenRouter (recommended — use any model: GPT-4, Claude, etc.)
OPENROUTER_API_KEY=your_openrouter_api_key_here
# Optional: model ID (default: arcee-ai/trinity-large-preview:free)
# OPENROUTER_MODEL=arcee-ai/trinity-large-preview:free
# When generating from artwork images, a vision model is used. Default: nvidia/nemotron-nano-12b-v2-vl:free (free). Override if needed:
# OPENROUTER_VISION_MODEL=nvidia/nemotron-nano-12b-v2-vl:free

# Option 2: Direct OpenAI (used if OPENROUTER_API_KEY is not set)
OPENAI_API_KEY=your_openai_api_key_here

# Etsy API Configuration (connect your store via OAuth in the app)
ETSY_API_KEY=your_etsy_api_key_here
# Optional: shared secret from Etsy app (some endpoints use "keystring:secret" for x-api-key)
ETSY_SHARED_SECRET=your_etsy_shared_secret_here
# Optional: fallback shop ID if OAuth doesn't return one (e.g. single-shop dev)
ETSY_SHOP_ID=your_etsy_shop_id_here

# Storage Configuration
# Local: filesystem. Vercel: vercel_blob (set BLOB_READ_WRITE_TOKEN in Vercel)
STORAGE_TYPE=filesystem
STORAGE_PATH=./uploads
# BLOB_READ_WRITE_TOKEN=  # Set in Vercel when using STORAGE_TYPE=vercel_blob

# Cleanup: delete design/PSD files after each successful mockup (saves disk space).
# Set to true if you only need the exported mockup images. Leave false if you
# use one design with multiple PSDs in the same session (files are deleted after first use).
# CLEANUP_SOURCE_AFTER_MOCKUP=true

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Auth (SaaS)
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000

# Database: set in .env.local for local (dev Postgres URL); set in Vercel/host for production
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Stripe (Plus tier subscriptions)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PLUS_PRICE_ID=price_...

# Admin: only this email can access /dashboard/admin (users + feedback)
ADMIN_EMAIL=your_admin_email@example.com
