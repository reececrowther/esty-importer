# Local development overrides (copy to .env.local)
# Next.js loads .env then .env.local; .env.local overrides. Use this so you never
# have to change .env when switching between local and production.
#
# Copy: cp .env.local.example .env.local
# Then set the values below. .env.local is gitignored.

# Local database: SQLite (file) or a dev Postgres URL.
# SQLite: use "file:./dev.db" — Prisma will use schema.sqlite.prisma automatically.
# Postgres: use a postgresql://... URL — Prisma will use schema.prisma (production uses this on the server).
DATABASE_URL="file:./dev.db"

# Local app URLs (required for auth and Etsy OAuth callback)
NEXTAUTH_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: keep using filesystem storage locally
# STORAGE_TYPE=filesystem
# STORAGE_PATH=./uploads

# Mockup engine: use "photopea" so mockups run in the browser (no Node canvas needed).
# Leave unset to use server-side canvas (requires: Node 18, npm install canvas).
# Recommended for local dev on Windows if canvas fails to build.
# NEXT_PUBLIC_MOCKUP_ENGINE=photopea
